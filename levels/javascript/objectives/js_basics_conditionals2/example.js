function spellNumber(number) {
  // Read the help section for info on what goes here!
}

// The lines of code below will help test your function when you click the
// "Play" button:
console.log(`Lets use spellNumber with 1 for inputs: ${spellNumber(1)}`);
console.log(`Lets use 4 for inputs: ${spellNumber(4)}`);
console.log(`Lets use 23 for inputs: ${spellNumber(23)}`);

// Ignore this line for now - it "exports" your function so TwilioQuest can
// exercise and validate it
module.exports = {
  spellNumber,
};
