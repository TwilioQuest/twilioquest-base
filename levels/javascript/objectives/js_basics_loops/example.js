// Check the help section for info on how to write this function!
function power(base, power) {
  let total = 1;
  let index = 0;

  while (false) {
    // Hmm, what should we put in our loop?
  }

  return total;
}

// The lines of code below will test your variables when you click the "Play"
// button. Check the output to ensure you're creating the variables you need!
console.log(`Lets use power with 2 & 3 for inputs: ${power(2, 3)}`);
console.log(`Lets use 2 & 4 for inputs: ${power(2, 4)}`);
console.log(`Lets use 4 & 3 for inputs: ${power(4, 3)}`);

// Ignore this line for now - it "exports" your function so TwilioQuest can
// exercise and validate it
module.exports = {
  power,
};
